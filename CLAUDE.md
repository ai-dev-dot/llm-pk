# CLAUDE.md

本文件是给 AI 协作者(Claude Code)在本项目中工作时遵循的约定。

面向人类读者的完整项目介绍与规范见 [README.md](README.md)。

## 项目定位

- 本项目存放「由大语言模型直接对战(PK)」的游戏集合,是全新项目,不继承任何既有业务代码;
- 每款游戏由多个 LLM 分别扮演对弈的各方角色,通过 Web 页面实时展示对局,并记录完整日志以支持回放与复盘。

## 新增游戏必须满足的五个约定

1. **规则明确** —— 规则无歧义,胜负由规则客观、自动判定;
2. **多角色** —— 至少双方,每个 LLM 各操作一个角色,互不共享状态;
3. **Web 展示** —— 对局通过 Web 页面实时展示,尽量展示模型思考过程;
4. **日志完整** —— 结构化记录对局全程(回合、操作、时间、决策依据),用于复盘分析;
5. **回放支持** —— 基于日志提供对局回放,可定位到任意回合。

## 与模型交互的原则(项目级,适用于所有 PK 游戏)

我们的玩家是「会思考的模型」,不是「能执行动作的机器人」。任何游戏都必须遵守:

1. **文本化(原则 A)** —— 局面一律以 ASCII/文本形态传给模型,禁止位图/截图/SVG(模型大多非多模态);
2. **单代码公证(原则 B)** —— 所有对弈方共用同一份裁判代码、同一个玩家适配器、同一份提示词模板(仅身份词差异);**唯一允许的不对称是红黑/各方所选模型不同**;
3. **上下文隔离(原则 C)** —— 各方会话互相独立,看不到对方历史、对方思考、裁判内部状态;是否回显"己方上一步思考"由配置开关决定;
4. **棋手式自由出招(原则 D)** —— 模型只看到局面与规则,自行提出行动(自由表达,如中文记谱/坐标),**不向其提供"合法行动清单"**;行动由确定性裁判代码解析+校验;不合规则即打回(解释原因、给予修正机会、**绝不枚举正确行动**);打回次数 = 规则掌握度的测评指标,记录并展示;累计打回超限判负。

设计展开实例见 `docs/superpowers/specs/2026-08-26-xiangqi-llm-pk-design.md` 的 §2/§6/§9。

## 开发约定

- 每个游戏自包含在 `games/<game_name>/` 目录下,先写 `rules.md`,再实现引擎与页面;
- 模型接入层提供统一接口,方便切换、对比不同模型;
- 对局日志采用统一的结构化格式,保证跨游戏可复盘、可回放。

## 沟通约定

- 与用户沟通一律使用**中文**;代码、技术术语、文件名可使用英文。